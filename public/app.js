const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const weekdays=['الأحد','الإثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'];
const typeNames={private_student_home:'خاص عند الطالب',private_sozan_home:'خاص عندي',online:'أونلاين',center_group:'مجموعة السنتر',own_group:'مجموعة خاصة'};
const expenseNames={work_transport:'مواصلات الشغل',books_printing:'كتب وطباعة',teaching_supplies:'أدوات تعليم',work_internet:'إنترنت الشغل',center_fees:'مصاريف السنتر',study_materials:'مواد دراسية',other_business:'مصروف شغل آخر',home:'البيت',food:'الأكل',personal_transport:'مواصلات شخصية',bills:'فواتير',children:'الأطفال',commitments:'التزامات',personal_shopping:'شراء شخصي',health:'الصحة',other_personal:'مصروف شخصي آخر'};
const incomeNames={course:'كورس',extra_group:'مجموعة إضافية',materials:'مواد تعليمية',bonus:'مكافأة',other:'أخرى'};
const expenseOptions={personal:[['home','البيت'],['food','الأكل'],['personal_transport','مواصلات شخصية'],['bills','فواتير'],['children','الأطفال'],['commitments','التزامات'],['personal_shopping','شراء شخصي'],['health','الصحة'],['other_personal','أخرى']],business:[['work_transport','مواصلات الشغل'],['books_printing','كتب وطباعة'],['teaching_supplies','أدوات تعليم'],['work_internet','إنترنت الشغل'],['center_fees','مصاريف السنتر'],['study_materials','مواد دراسية'],['other_business','أخرى']]};
let dashboardCache=null,scheduleCache=[],settingsCache={},studentSummaryCache=[],activityFilter='all',activityTimer=null,toastTimer=null;

const localDate=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const monthNow=()=>localDate().slice(0,7);
const money=p=>`${(Number(p||0)/100).toLocaleString('ar-EG',{maximumFractionDigits:2})} ج`;
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const fmtDate=d=>{if(!d)return'';try{return new Intl.DateTimeFormat('ar-EG',{day:'numeric',month:'short'}).format(new Date(`${String(d).slice(0,10)}T12:00:00`))}catch{return d}};
const fmtDateTime=d=>{if(!d)return'';try{return new Intl.DateTimeFormat('ar-EG',{day:'numeric',month:'short',hour:'numeric',minute:'2-digit'}).format(new Date(String(d).replace(' ','T')+'Z'))}catch{return d}};

async function api(path,options={}){const res=await fetch(path,{credentials:'same-origin',...options,headers:options.body instanceof FormData?(options.headers||{}):{'content-type':'application/json',...(options.headers||{})}});if(res.status===401){showLogin();throw new Error('AUTH_REQUIRED')}const data=res.status===204?{}:await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||`HTTP ${res.status}`);return data}
function showLogin(){$('#loginView').classList.remove('hidden');$('#appView').classList.add('hidden')}
function showApp(){$('#loginView').classList.add('hidden');$('#appView').classList.remove('hidden')}
function openDialog(id){const d=$(`#${id}`);if(!d||d.open)return;if(d.showModal)d.showModal();else d.setAttribute('open','')}
function closeDialog(id){const d=$(`#${id}`);if(d?.close)d.close();else d?.removeAttribute('open')}
document.addEventListener('click',e=>{const b=e.target.closest('.close-dialog');if(b){const d=b.closest('dialog');if(d?.close)d.close()}});

$('#loginForm').addEventListener('submit',async e=>{e.preventDefault();$('#loginError').textContent='';try{await api('/api/login',{method:'POST',body:JSON.stringify({passcode:$('#passcode').value})});showApp();await refreshAll()}catch(err){if(err.message!=='AUTH_REQUIRED')$('#loginError').textContent=err.message}});
$('#logoutBtn').addEventListener('click',async()=>{try{await api('/api/logout',{method:'POST'})}catch{}showLogin()});
$$('.nav-btn').forEach(btn=>btn.addEventListener('click',()=>switchView(btn.dataset.view)));
function switchView(name){$$('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.view===name));$$('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${name}`));$('#pageTitle').textContent={today:'اليوم',money:'فلوسي',schedule:'جدولي',me:'أنا'}[name];if(name==='money')loadMoney();if(name==='schedule')loadSchedule();if(name==='me')loadMe()}
async function refreshAll(){await Promise.all([loadSettings(),loadSchedule(),loadStudentSummaries()]);await loadDashboard();await Promise.all([loadMoney(),loadMe()])}

async function loadDashboard(){const d=await api(`/api/v4/dashboard?date=${localDate()}`);dashboardCache=d;const s=d.summary,h=new Date().getHours(),hello=h<12?'صباح الخير':'مساء الخير',display=settingsCache.sozan_display_name||'سوزان';$('#greetingText').textContent=`${hello} يا ${display}`;$('#monthLeft').textContent=money(s.month_left_pence);$('#receivedValue').textContent=money(s.received_total_pence);$('#spentValue').textContent=money(s.expenses_pence);$('#outstandingValue').textContent=money(s.outstanding_pence);$('#outstandingCount').textContent=s.outstanding_pence>0?'اضغطي للمراجعة':'تمام';renderToday(d.sessions||[])}
function renderToday(rows){const el=$('#todaySessions');if(!rows.length){el.innerHTML='<div class="empty">مفيش حصص مسجلة لليوم.<br>من «جدولي» أضيفي الطالب مرة واحدة فقط.</div>';return}
el.innerHTML=rows.map(r=>{const net=Math.max(0,Number(r.gross_pence||0)-Number(r.center_cut_pence||0)),done=r.status==='completed',cancelled=r.status==='cancelled',paid=Number(r.paid_pence||0),due=Number(r.outstanding_pence||0);const status=cancelled?'اتلغت':done?(due>0?`تمت · باقي ${money(due)}`:'تمت واتدفعت'):'مجدولة';const moved=r.rescheduled_to_date?`<span class="micro-badge">منقولة</span>`:'';let primary='';if(!done&&!cancelled)primary=`<button class="session-primary" data-action="complete-paid" data-id="${r.id}">✓ تمت واتدفعت</button>`;else if(done&&due>0)primary=`<button class="session-primary soft-primary" data-action="collect" data-id="${r.id}">تم التحصيل</button>`;return `<div class="session-card"><div class="session-head"><button class="student-name-btn" data-student="${r.student_id||''}" ${r.student_id?'':'disabled'}><div class="session-title">${esc(r.title)}</div><div class="session-meta">${esc(r.start_time)} · ${esc(typeNames[r.session_type]||r.session_type)} ${moved}</div></button><span class="amount-pill">${money(net)}</span></div><div class="session-status-row"><span>${status}</span>${paid>0?`<small>مستلم ${money(paid)}</small>`:''}</div><div class="session-footer">${primary}<button class="more-btn" data-more-session="${r.id}" aria-label="خيارات الحصة">•••</button></div></div>`}).join('');
el.querySelectorAll('[data-action]').forEach(b=>b.addEventListener('click',()=>runOccurrenceAction(b.dataset.id,b.dataset.action)));el.querySelectorAll('[data-more-session]').forEach(b=>b.addEventListener('click',()=>openSessionActions(b.dataset.moreSession)));el.querySelectorAll('[data-student]').forEach(b=>{if(b.dataset.student)b.addEventListener('click',()=>openStudentProfile(b.dataset.student))})}
async function runOccurrenceAction(id,action){try{await api(`/api/v3/occurrences/${id}/${action}`,{method:'POST',body:JSON.stringify({paid_at:localDate()})});showToast(action==='cancel'?'تم إلغاء حصة اليوم':action==='restore'?'رجعت الحصة':action==='reopen'?'رجعت لمجدولة':'تم التحديث');closeDialog('sessionActionsDialog');await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}}
function openSessionActions(id){const r=dashboardCache?.sessions?.find(x=>String(x.id)===String(id));if(!r)return;$('#sessionActionName').textContent=r.title;const list=$('#sessionActionList');const buttons=[];if(r.status==='scheduled'){buttons.push(['تمت ولسه','complete-unpaid'],['نقل الحصة دي فقط','reschedule'],['إلغاء حصة اليوم','cancel'])}else if(r.status==='completed'){if(Number(r.paid_pence||0)>0)buttons.push(['تصحيح الدفع','correct']);buttons.push(['رجوع لمجدولة','reopen'])}else if(r.status==='cancelled'){buttons.push(['إرجاع الحصة','restore'])}list.innerHTML=buttons.map(([label,act])=>`<button class="action-row" data-session-menu="${act}" data-id="${r.id}">${label}<span>‹</span></button>`).join('');list.querySelectorAll('[data-session-menu]').forEach(b=>b.addEventListener('click',()=>{const act=b.dataset.sessionMenu;if(act==='reschedule'){closeDialog('sessionActionsDialog');openReschedule(id)}else if(act==='correct'){closeDialog('sessionActionsDialog');openPaymentCorrection(id)}else runOccurrenceAction(id,act)}));openDialog('sessionActionsDialog')}
async function openReschedule(id,row=null){let r=row||dashboardCache?.sessions?.find(x=>String(x.id)===String(id));if(!r){try{r=(await api(`/api/v4/occurrences/${id}`)).occurrence}catch(err){showToast(err.message,null,true);return}}const f=$('#rescheduleForm');f.reset();f.occurrence_id.value=id;f.date.value=r.session_date||localDate();f.time.value=r.start_time||'09:00';$('#rescheduleName').textContent=r.title;openDialog('rescheduleDialog')}
$('#rescheduleForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.occurrence_id;delete b.occurrence_id;try{const d=await api(`/api/v3/occurrences/${id}/reschedule`,{method:'POST',body:JSON.stringify(b)});closeDialog('rescheduleDialog');showToast(d.restored_original?'رجعت للموعد الأصلي':'تم نقل الحصة');await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});
async function openPaymentCorrection(id,row=null){let r=row||dashboardCache?.sessions?.find(x=>String(x.id)===String(id));if(!r){try{r=(await api(`/api/v4/occurrences/${id}`)).occurrence}catch(err){showToast(err.message,null,true);return}}const f=$('#paymentCorrectionForm');f.reset();f.occurrence_id.value=id;f.received_total.value=(Number(r.paid_pence||0)/100).toFixed(2);$('#paymentCorrectionName').textContent=r.title;$('#paymentCorrectionHint').textContent=`المستحق ${money(r.earned_pence)} · المسجل مستلم ${money(r.paid_pence)}`;openDialog('paymentCorrectionDialog')}
$('#paymentCorrectionForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.occurrence_id;delete b.occurrence_id;try{await api(`/api/v3/occurrences/${id}/correct-payment`,{method:'POST',body:JSON.stringify(b)});closeDialog('paymentCorrectionDialog');showToast('تم تصحيح التحصيل');await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});

async function loadStudentSummaries(){try{const d=await api('/api/v4/students/summary');studentSummaryCache=d.students||[];renderStudentCollection(studentSummaryCache);fillReceiptStudents()}catch(err){if(err.message!=='AUTH_REQUIRED')console.error(err)}}
function renderStudentCollection(items){const el=$('#studentCollectionList');if(!el)return;if(!items.length){el.innerHTML='<div class="empty">أضيفي أول طالب من «جدولي».</div>';return}const show=[...items].sort((a,b)=>b.outstanding_pence-a.outstanding_pence).slice(0,6);el.innerHTML=show.map(s=>{const state=s.outstanding_pence>0?`عليه ${money(s.outstanding_pence)}`:s.credit_pence>0?`رصيد مقدم ${money(s.credit_pence)}`:'مسدد';return `<button class="student-row" data-student="${s.id}"><div class="student-avatar">${esc(s.name.slice(0,1))}</div><div class="student-row-main"><strong>${esc(s.name)}</strong><small>${esc(s.guardian_name||'ولي الأمر غير مسجل')} ${s.last_payment_date?`· آخر دفع ${fmtDate(s.last_payment_date)}`:''}</small></div><span class="student-state ${s.outstanding_pence>0?'due':s.credit_pence>0?'credit':'ok'}">${state}</span></button>`}).join('');el.querySelectorAll('[data-student]').forEach(b=>b.addEventListener('click',()=>openStudentProfile(b.dataset.student)))}
function fillReceiptStudents(selected){const sel=$('#receiptStudent');if(!sel)return;sel.innerHTML='<option value="">اختاري الطالب</option>'+studentSummaryCache.map(s=>`<option value="${s.id}">${esc(s.name)}${s.outstanding_pence>0?` — عليه ${money(s.outstanding_pence)}`:s.credit_pence>0?` — رصيد ${money(s.credit_pence)}`:''}</option>`).join('');if(selected)sel.value=String(selected);updateReceiptHint()}
$('#receiptStudent').addEventListener('change',updateReceiptHint);
function updateReceiptHint(){const id=Number($('#receiptStudent').value),s=studentSummaryCache.find(x=>Number(x.id)===id),el=$('#receiptStudentHint');if(!s){el.innerHTML='اختاري الطالب، والبرنامج هيوزع المبلغ على أقدم الحصص المستحقة تلقائيًا.';return}if(s.outstanding_pence>0)el.innerHTML=`على <strong>${esc(s.name)}</strong> حاليًا ${money(s.outstanding_pence)}${s.outstanding_count?` من ${s.outstanding_count} حصة`:''}. أي زيادة ستبقى رصيدًا مقدمًا.`;else if(s.credit_pence>0)el.innerHTML=`${esc(s.name)} مسدد وعنده رصيد مقدم ${money(s.credit_pence)}.`;else el.innerHTML=`${esc(s.name)} مسدد حاليًا. أي مبلغ جديد سيبقى رصيدًا مقدمًا للحصص القادمة.`}
function openReceipt(studentId=null,id=null){const f=$('#receiptForm');f.reset();f.id.value='';f.received_at.value=localDate();$('#receiptDialogTitle').textContent='قبضت فلوس';$('#deleteReceiptBtn').classList.add('hidden');fillReceiptStudents(studentId);if(id)loadReceiptForEdit(id);else openDialog('receiptDialog')}
async function loadReceiptForEdit(id){try{const d=await api(`/api/v4/receipts/${id}`),r=d.receipt,f=$('#receiptForm');f.id.value=r.id;fillReceiptStudents(r.student_id);f.amount.value=(Number(r.amount_pence)/100).toFixed(2);f.received_at.value=r.received_at;f.payment_method.value=r.payment_method;f.note.value=r.note||'';$('#receiptDialogTitle').textContent='تعديل الاستلام';$('#deleteReceiptBtn').classList.toggle('hidden',!!r.deleted_at);openDialog('receiptDialog')}catch(err){showToast(err.message,null,true)}}
$('#receiptForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.id;delete b.id;try{const d=await api(id?`/api/v4/receipts/${id}`:'/api/v4/receipts',{method:id?'PATCH':'POST',body:JSON.stringify(b)});closeDialog('receiptDialog');const extra=Number(d.credit_pence||0)>0?` · رصيد مقدم ${money(d.credit_pence)}`:'';showToast(id?'تم تعديل الاستلام':`تم تسجيل الاستلام${extra}`);await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});
$('#deleteReceiptBtn').addEventListener('click',async()=>{const id=$('#receiptForm [name=id]').value;if(!id||!confirm('حذف هذا الاستلام؟ المستحقات المرتبطة به هترجع تلقائيًا.'))return;try{await api(`/api/v4/receipts/${id}`,{method:'DELETE'});closeDialog('receiptDialog');showToast('تم حذف الاستلام',()=>restoreEntity('receipt',id));await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});

async function openStudentProfile(id){try{const d=await api(`/api/v4/students/${id}/account`),s=d.student;const body=$('#studentProfileBody');body.innerHTML=`<div class="sheet-head"><div><p class="kicker">حساب الطالب</p><h3>${esc(s.name)}</h3></div><button type="button" class="close-dialog">×</button></div><div class="student-summary-grid"><div><span>عليه الآن</span><strong>${money(s.outstanding_pence)}</strong></div><div><span>رصيد مقدم</span><strong>${money(s.credit_pence)}</strong></div><div><span>آخر دفع</span><strong>${s.last_payment_date?money(s.last_payment_pence):'—'}</strong><small>${s.last_payment_date?fmtDate(s.last_payment_date):''}</small></div></div><button class="btn primary wide" id="studentReceiptBtn">+ قبضت فلوس من ${esc(s.guardian_name||s.name)}</button><div class="profile-section"><div class="panel-head"><h4>بيانات بسيطة</h4><button class="text-btn" id="toggleStudentEdit">تعديل</button></div><div id="studentInfoView" class="info-lines"><div><span>ولي الأمر</span><strong>${esc(s.guardian_name||'غير مسجل')}</strong></div><div><span>الهاتف</span><strong>${esc(s.guardian_phone||'غير مسجل')}</strong></div><div><span>المستوى</span><strong>${esc(s.level||'غير مسجل')}</strong></div></div><form id="studentEditForm" class="stack-form hidden"><label>اسم الطالب<input name="name" value="${esc(s.name)}" required /></label><label>اسم ولي الأمر<input name="guardian_name" value="${esc(s.guardian_name||'')}" /></label><label>رقم الهاتف<input name="guardian_phone" value="${esc(s.guardian_phone||'')}" inputmode="tel" /></label><label>المستوى<input name="level" value="${esc(s.level||'')}" /></label><label>ملاحظات<input name="notes" value="${esc(s.notes||'')}" /></label><button class="btn secondary" type="submit">حفظ البيانات</button></form></div><div class="profile-section"><h4>المواعيد</h4><div class="schedule-mini">${(s.schedules||[]).length?s.schedules.map(x=>`<span>${weekdays[x.weekday]} · ${esc(x.start_time)} · ${esc(typeNames[x.session_type]||'حصة')}</span>`).join(''):'<span class="subtle">لا توجد مواعيد نشطة</span>'}</div></div><div class="profile-section"><h4>آخر الحركة</h4><div class="timeline-mini">${renderStudentTimeline(d.timeline||[])}</div></div>`;body.querySelector('#studentReceiptBtn').addEventListener('click',()=>{closeDialog('studentDialog');openReceipt(id)});body.querySelector('#toggleStudentEdit').addEventListener('click',()=>{$('#studentInfoView').classList.toggle('hidden');$('#studentEditForm').classList.toggle('hidden')});body.querySelector('#studentEditForm').addEventListener('submit',async e=>{e.preventDefault();try{await api(`/api/v3/students/${id}`,{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target).entries()))});showToast('اتحفظت بيانات الطالب');await loadStudentSummaries();await openStudentProfile(id)}catch(err){showToast(err.message,null,true)}});openDialog('studentDialog')}catch(err){showToast(err.message,null,true)}}
function renderStudentTimeline(items){if(!items.length)return'<div class="empty">لسه مفيش تاريخ كفاية.</div>';return items.slice(0,12).map(x=>x.kind==='receipt'?`<div class="timeline-row"><span class="dot income-dot"></span><div><strong>استلام ${money(x.amount_pence)}</strong><small>${fmtDate(x.date)}${x.deleted_at?' · محذوف':''}</small></div></div>`:`<div class="timeline-row"><span class="dot session-dot"></span><div><strong>حصة · ${x.status==='completed'?(x.outstanding_pence>0?'لسه عليها':'مدفوعة'):x.status==='cancelled'?'ملغاة':'مجدولة'}</strong><small>${fmtDate(x.date)} · ${money(x.earned_pence)}</small></div></div>`).join('')}
function openAllStudents(){const body=$('#studentProfileBody');body.innerHTML=`<div class="sheet-head"><div><p class="kicker">الطلاب</p><h3>مين دفع ومين لسه؟</h3></div><button type="button" class="close-dialog">×</button></div><div class="student-list">${studentSummaryCache.map(s=>`<button class="student-row" data-all-student="${s.id}"><div class="student-avatar">${esc(s.name.slice(0,1))}</div><div class="student-row-main"><strong>${esc(s.name)}</strong><small>${esc(s.guardian_name||'ولي الأمر غير مسجل')}</small></div><span class="student-state ${s.outstanding_pence>0?'due':s.credit_pence>0?'credit':'ok'}">${s.outstanding_pence>0?`عليه ${money(s.outstanding_pence)}`:s.credit_pence>0?`رصيد ${money(s.credit_pence)}`:'مسدد'}</span></button>`).join('')}</div>`;body.querySelectorAll('[data-all-student]').forEach(b=>b.addEventListener('click',()=>openStudentProfile(b.dataset.allStudent)));openDialog('studentDialog')}

async function loadMoney(){try{const [outstanding,activity]=await Promise.all([api('/api/v3/outstanding'),api('/api/v4/activity?limit=5')]),s=dashboardCache?.summary;if(s){$('#earnedValue').textContent=money(s.earned_pence);$('#moneyReceivedValue').textContent=money(s.received_total_pence);$('#moneySpentValue').textContent=money(s.expenses_pence);$('#moneyOutstandingValue').textContent=money(s.outstanding_pence)}renderBreakdown(dashboardCache?.expense_categories||[]);renderRecentActivity(activity.events||[])}catch(err){if(err.message!=='AUTH_REQUIRED')console.error(err)}}
function renderBreakdown(rows){const el=$('#expenseBreakdown');if(!rows.length){el.innerHTML='<div class="empty">لسه مفيش مصروفات مسجلة هذا الشهر.</div>';return}const max=Math.max(...rows.map(r=>Number(r.amount_pence||0)),1);el.innerHTML=rows.slice(0,7).map(r=>`<div class="breakdown-row"><div class="breakdown-top"><span>${esc(expenseNames[r.category]||r.category)}</span><strong>${money(r.amount_pence)}</strong></div><div class="bar-track"><div class="bar-fill" style="width:${Math.max(5,Math.round(Number(r.amount_pence||0)/max*100))}%"></div></div></div>`).join('')}

function renderRecentActivity(events){const el=$('#recentActivity');if(!events.length){el.innerHTML='<div class="empty">لسه مفيش حركات.</div>';return}el.innerHTML=events.map(activityRowHtml).join('');bindActivityActions(el)}
function activityRowHtml(e){const icon={expense:'−',other_income:'+',receipt:'↓',session:'▦',occurrence:'✓',payment:'↓',student:'◉',settings:'⚙'}[e.entity_type]||'•';const canOpen=e.entity_type!=='settings'&&e.entity_id;return `<div class="activity-row"><span class="activity-icon ${esc(e.entity_type)}">${icon}</span><div class="activity-main"><strong>${esc(e.title)}</strong><small>${esc(e.detail||'')} · ${fmtDateTime(e.created_at)}</small></div>${canOpen?`<button class="more-btn small-more" data-activity-menu="${e.id}" data-etype="${e.entity_type}" data-eid="${e.entity_id}" data-latest="${esc(e.latest_action||e.action)}">•••</button>`:''}</div>`}
function bindActivityActions(root){root.querySelectorAll('[data-activity-menu]').forEach(b=>b.addEventListener('click',()=>openActivityMenu({eventId:b.dataset.activityMenu,type:b.dataset.etype,id:b.dataset.eid,latest:b.dataset.latest})))}
function openActivityMenu(x){
  const moneyType=['expense','other_income','receipt'].includes(x.type);
  if(moneyType){const title=x.type==='expense'?'مصروف':x.type==='other_income'?'دخل آخر':'استلام';$('#sessionActionName').textContent=title;const deleted=x.latest==='deleted';$('#sessionActionList').innerHTML=deleted?`<button class="action-row" id="activityRestore">استرجاع<span>‹</span></button>`:`<button class="action-row" id="activityEdit">تعديل<span>‹</span></button><button class="action-row danger-row" id="activityDelete">حذف<span>‹</span></button>`;if(deleted)$('#activityRestore').addEventListener('click',async()=>{await restoreEntity(x.type,x.id);closeDialog('sessionActionsDialog')});else{$('#activityEdit').addEventListener('click',()=>{closeDialog('sessionActionsDialog');if(x.type==='expense')openExpense(x.id);if(x.type==='other_income')openIncome(x.id);if(x.type==='receipt')openReceipt(null,x.id)});$('#activityDelete').addEventListener('click',async()=>{closeDialog('sessionActionsDialog');await deleteEntityFromActivity(x.type,x.id)})}openDialog('sessionActionsDialog');return}
  if(x.type==='student'){closeDialog('activityDialog');openStudentProfile(x.id);return}
  if(x.type==='session'){const found=scheduleCache.find(z=>String(z.id)===String(x.id));if(found){closeDialog('activityDialog');openSessionDialog(x.id)}else showToast('الحصة دي متوقفة أو غير موجودة');return}
  if(x.type==='occurrence'||x.type==='payment'){openOccurrenceManage(x.id);return}
}
async function openOccurrenceManage(id){try{const r=(await api(`/api/v4/occurrences/${id}`)).occurrence;$('#sessionActionName').textContent=r.title;const acts=[];if(r.status==='completed'){if(Number(r.paid_pence||0)>0)acts.push(['تصحيح الدفع','correct']);acts.push(['رجوع لمجدولة','reopen'])}else if(r.status==='scheduled'){acts.push(['نقل الحصة','reschedule'],['إلغاء الحصة','cancel'])}else if(r.status==='cancelled')acts.push(['إرجاع الحصة','restore']);$('#sessionActionList').innerHTML=acts.map(([l,a])=>`<button class="action-row" data-past-action="${a}">${l}<span>‹</span></button>`).join('');$('#sessionActionList').querySelectorAll('[data-past-action]').forEach(b=>b.addEventListener('click',()=>{const a=b.dataset.pastAction;closeDialog('sessionActionsDialog');if(a==='correct')openPaymentCorrection(id,r);else if(a==='reschedule')openReschedule(id,r);else runOccurrenceAction(id,a)}));openDialog('sessionActionsDialog')}catch(err){showToast(err.message,null,true)}}
async function deleteEntityFromActivity(type,id){if(!confirm('حذف الحركة؟ كل الأرقام المرتبطة هتتحدث تلقائيًا.'))return;try{const endpoint=type==='expense'?`/api/v3/expenses/${id}`:type==='other_income'?`/api/v3/other-income/${id}`:`/api/v4/receipts/${id}`;await api(endpoint,{method:'DELETE'});showToast('تم الحذف',()=>restoreEntity(type,id));await refreshFinancialViews();await loadActivity()}catch(err){showToast(err.message,null,true)}}
async function restoreEntity(type,id){try{await api(`/api/v4/restore/${type}/${id}`,{method:'POST'});showToast('تم الاسترجاع');await refreshFinancialViews();await loadActivity()}catch(err){showToast(err.message,null,true)}}

async function loadActivity(){try{const q=$('#activitySearch')?.value?.trim()||'';const d=await api(`/api/v4/activity?type=${encodeURIComponent(activityFilter)}&q=${encodeURIComponent(q)}&limit=120`);const el=$('#activityFullList');el.innerHTML=d.events?.length?d.events.map(activityRowHtml).join(''):'<div class="empty">مفيش نتائج.</div>';bindActivityActions(el)}catch(err){if(err.message!=='AUTH_REQUIRED')showToast(err.message,null,true)}}
function openActivity(){openDialog('activityDialog');loadActivity()}
$('#activitySearch').addEventListener('input',()=>{clearTimeout(activityTimer);activityTimer=setTimeout(loadActivity,250)});
$$('[data-activity-filter]').forEach(b=>b.addEventListener('click',()=>{$$('[data-activity-filter]').forEach(x=>x.classList.toggle('active',x===b));activityFilter=b.dataset.activityFilter;loadActivity()}));

function openExpense(id=null){const f=$('#expenseForm');f.reset();f.id.value='';f.date.value=localDate();setExpenseScope('personal');$('#expenseDialogTitle').textContent='سجلّي مصروف';$('#deleteExpenseBtn').classList.add('hidden');if(id)loadExpenseForEdit(id);else openDialog('expenseDialog')}
async function loadExpenseForEdit(id){try{const d=await api(`/api/v3/expenses/${id}`),e=d.expense,f=$('#expenseForm');f.id.value=e.id;f.amount.value=(Number(e.amount_pence)/100).toFixed(2);f.date.value=e.expense_date;f.note.value=e.note||'';setExpenseScope(e.scope);f.category.value=e.category;$('#expenseDialogTitle').textContent='تعديل المصروف';$('#deleteExpenseBtn').classList.toggle('hidden',!!e.deleted_at);openDialog('expenseDialog')}catch(err){showToast(err.message,null,true)}}
function setExpenseScope(scope){$('#expenseForm [name=scope]').value=scope;$$('#expenseScope button').forEach(b=>b.classList.toggle('active',b.dataset.scope===scope));$('#expenseForm [name=category]').innerHTML=expenseOptions[scope].map(([v,l])=>`<option value="${v}">${l}</option>`).join('')}
$$('#expenseScope button').forEach(b=>b.addEventListener('click',()=>setExpenseScope(b.dataset.scope)));
$('#expenseForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.id;delete b.id;try{await api(id?`/api/v3/expenses/${id}`:'/api/v3/expenses',{method:id?'PATCH':'POST',body:JSON.stringify(b)});closeDialog('expenseDialog');showToast(id?'اتعدل المصروف':'اتسجل المصروف');await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});
$('#deleteExpenseBtn').addEventListener('click',async()=>{const id=$('#expenseForm [name=id]').value;if(id)await deleteEntityFromActivity('expense',id);closeDialog('expenseDialog')});

function openIncome(id=null){const f=$('#incomeForm');f.reset();f.id.value='';f.date.value=localDate();$('#incomeDialogTitle').textContent='دخل غير الحصص';$('#deleteIncomeBtn').classList.add('hidden');if(id)loadIncomeForEdit(id);else openDialog('incomeDialog')}
async function loadIncomeForEdit(id){try{const d=await api(`/api/v3/other-income/${id}`),i=d.income,f=$('#incomeForm');f.id.value=i.id;f.amount.value=(Number(i.amount_pence)/100).toFixed(2);f.category.value=i.category;f.date.value=i.income_date;f.note.value=i.note||'';$('#incomeDialogTitle').textContent='تعديل الدخل';$('#deleteIncomeBtn').classList.toggle('hidden',!!i.deleted_at);openDialog('incomeDialog')}catch(err){showToast(err.message,null,true)}}
$('#incomeForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.id;delete b.id;try{await api(id?`/api/v3/other-income/${id}`:'/api/v3/other-income',{method:id?'PATCH':'POST',body:JSON.stringify(b)});closeDialog('incomeDialog');showToast(id?'اتعدل الدخل':'اتسجل الدخل');await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});
$('#deleteIncomeBtn').addEventListener('click',async()=>{const id=$('#incomeForm [name=id]').value;if(id)await deleteEntityFromActivity('other_income',id);closeDialog('incomeDialog')});

async function loadSchedule(){try{const d=await api('/api/v3/sessions');scheduleCache=d.sessions||[];const el=$('#scheduleList');if(!scheduleCache.length){el.innerHTML='<div class="empty">الجدول فاضي.<br>أضيفي أول طالب أو مجموعة.</div>';return}el.innerHTML=scheduleCache.map(s=>`<div class="schedule-card"><div class="schedule-main"><button class="student-name-btn" data-schedule-student="${s.student_id||''}" ${s.student_id?'':'disabled'}><strong>${esc(s.title)}</strong><div class="schedule-meta">${weekdays[s.weekday]} · ${esc(s.start_time)} · ${esc(typeNames[s.session_type]||s.session_type)}</div></button><span class="amount-pill">${money(s.price_pence)}</span></div><div class="schedule-actions"><button class="edit-btn" data-edit-session="${s.id}">تعديل المواعيد القادمة</button></div></div>`).join('');el.querySelectorAll('[data-edit-session]').forEach(b=>b.addEventListener('click',()=>openSessionDialog(b.dataset.editSession)));el.querySelectorAll('[data-schedule-student]').forEach(b=>{if(b.dataset.scheduleStudent)b.addEventListener('click',()=>openStudentProfile(b.dataset.scheduleStudent))})}catch(err){if(err.message!=='AUTH_REQUIRED')console.error(err)}}
function openSessionDialog(id=null){const f=$('#sessionForm');f.reset();f.id.value='';f.student_id.value='';f.duration_minutes.value=60;f.travel_minutes.value=0;f.student_count.value=1;f.center_cut_percent.value=0;f.price_basis.value='total_session';$('#stopSessionBtn').classList.add('hidden');$('#sessionEditHint').classList.add('hidden');$('#sessionDialogTitle').textContent='طالب / مجموعة جديدة';if(id){const s=scheduleCache.find(x=>String(x.id)===String(id));if(!s)return;f.id.value=s.id;f.student_id.value=s.student_id||'';f.title.value=s.title;f.session_type.value=s.session_type;f.weekday.value=s.weekday;f.start_time.value=s.start_time;f.price.value=(Number(s.price_pence||0)/100).toFixed(2);f.duration_minutes.value=s.duration_minutes;f.travel_minutes.value=s.travel_minutes;f.student_count.value=s.student_count;f.center_cut_percent.value=s.center_cut_percent;f.price_basis.value=s.price_basis||'total_session';f.location.value=s.location||'';$('#sessionDialogTitle').textContent='تعديل الحصة';$('#stopSessionBtn').classList.remove('hidden');$('#sessionEditHint').classList.remove('hidden')}openDialog('sessionDialog')}
$('#sessionForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries()),id=b.id;delete b.id;['weekday','duration_minutes','travel_minutes','student_count','center_cut_percent'].forEach(k=>b[k]=Number(b[k]||0));b.student_name=b.title;try{await api(id?`/api/v3/sessions/${id}`:'/api/v3/sessions',{method:id?'PATCH':'POST',body:JSON.stringify(b)});closeDialog('sessionDialog');showToast(id?'اتحفظ التعديل':'اتضافت الحصة');await Promise.all([loadSchedule(),loadStudentSummaries(),loadDashboard(),loadActivity()])}catch(err){showToast(err.message,null,true)}});
$('#stopSessionBtn').addEventListener('click',async()=>{const id=$('#sessionForm [name=id]').value;if(!id||!confirm('إيقاف الحصة من الجدول؟ التاريخ القديم يفضل محفوظ.'))return;try{await api(`/api/v3/sessions/${id}`,{method:'DELETE'});closeDialog('sessionDialog');showToast('تم إيقاف الحصة');await Promise.all([loadSchedule(),loadDashboard(),loadActivity()])}catch(err){showToast(err.message,null,true)}});

async function loadMe(){try{const [insights,cash,review]=await Promise.all([api(`/api/v3/insights?date=${localDate()}`),api('/api/v3/cash-check'),api('/api/v4/review')]);$('#expectedBalanceValue').textContent=money(cash.expected_balance_pence);renderInsights(insights.insights||[]);renderLastCash(cash.last_check);renderReview(review.items||[])}catch(err){if(err.message!=='AUTH_REQUIRED')console.error(err)}}
function renderInsights(items){$('#insightsList').innerHTML=items.map(i=>`<div class="insight ${esc(i.tone||'info')}"><strong>${esc(i.title)}</strong><p>${esc(i.text)}</p></div>`).join('')}
function renderReview(items){const el=$('#reviewList');if(!items.length){el.innerHTML='<div class="all-good"><strong>كل شيء متناسق</strong><span>مفيش حاجة واضحة محتاجة تصحيح دلوقتي.</span></div>';return}el.innerHTML=items.map((x,i)=>`<button class="review-item ${esc(x.tone||'info')}" data-review="${i}"><strong>${esc(x.title)}</strong><span>${esc(x.text)}</span></button>`).join('');el.querySelectorAll('[data-review]').forEach(b=>b.addEventListener('click',()=>{const x=items[Number(b.dataset.review)];if(x.student_id)openStudentProfile(x.student_id);else if(x.entity_type==='expense'&&x.entity_ids?.[0])openExpense(x.entity_ids[0]);else openActivity()}))}
function renderLastCash(last){const el=$('#lastCashCheck');if(!last){el.textContent='';return}const diff=Number(last.difference_pence||0);el.textContent=diff<0?`آخر مراجعة: فيه حوالي ${money(Math.abs(diff))} صرف غير مسجل.`:diff>0?`آخر مراجعة: الموجود أعلى من المسجل بحوالي ${money(diff)}.`:'آخر مراجعة كانت متطابقة.'}
$('#cashForm').addEventListener('submit',async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target).entries());b.date=localDate();try{const d=await api('/api/v3/cash-check',{method:'POST',body:JSON.stringify(b)});closeDialog('cashDialog');showToast(d.unrecorded_spending_pence>0?`فيه ${money(d.unrecorded_spending_pence)} صرف غير مسجل`:d.unrecorded_income_pence>0?`فيه ${money(d.unrecorded_income_pence)} زيادة عن المسجل`:'الحساب متطابق');await loadMe()}catch(err){showToast(err.message,null,true)}});
async function loadSettings(){try{const d=await api('/api/v3/settings');settingsCache=d.settings||{};const f=$('#settingsForm');f.opening_balance.value=(Number(settingsCache.opening_balance_pence||0)/100).toFixed(2);f.sozan_display_name.value=settingsCache.sozan_display_name||'سوزان'}catch(err){if(err.message!=='AUTH_REQUIRED')console.error(err)}}
$('#settingsForm').addEventListener('submit',async e=>{e.preventDefault();try{await api('/api/v3/settings',{method:'PATCH',body:JSON.stringify(Object.fromEntries(new FormData(e.target).entries()))});showToast('اتحفظت الإعدادات');await loadSettings();await refreshFinancialViews()}catch(err){showToast(err.message,null,true)}});

async function refreshFinancialViews(){await Promise.all([loadDashboard(),loadStudentSummaries()]);await Promise.all([loadMoney(),loadMe(),loadActivity()])}
function showToast(msg,action=null,bad=false){clearTimeout(toastTimer);const t=$('#toast'),text=$('#toastText'),btn=$('#toastAction');text.textContent=msg;t.classList.toggle('bad',bad);if(action){btn.textContent='تراجع';btn.classList.remove('hidden');btn.onclick=async()=>{btn.classList.add('hidden');await action();t.classList.remove('show')}}else{btn.classList.add('hidden');btn.onclick=null}t.classList.add('show');toastTimer=setTimeout(()=>t.classList.remove('show'),action?5500:2800)}

$('#outstandingCard').addEventListener('click',()=>switchView('money'));
$('#quickReceiptBtn').addEventListener('click',()=>openReceipt());$('#moneyReceiptBtn').addEventListener('click',()=>openReceipt());
$('#quickExpenseBtn').addEventListener('click',()=>openExpense());$('#moneyExpenseBtn').addEventListener('click',()=>openExpense());
$('#otherIncomeBtn').addEventListener('click',()=>openIncome());$('#addSessionBtn').addEventListener('click',()=>openSessionDialog());
$('#cashCheckBtn').addEventListener('click',()=>openDialog('cashDialog'));
$('#activityBtn').addEventListener('click',openActivity);$('#showActivityBtn').addEventListener('click',openActivity);$('#openActivityFromReview').addEventListener('click',openActivity);$('#showAllStudentsBtn').addEventListener('click',openAllStudents);

if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});
(async()=>{try{await api(`/api/v4/dashboard?date=${localDate()}`);showApp();await refreshAll()}catch(err){if(err.message==='AUTH_REQUIRED')showLogin();else{showLogin();$('#loginError').textContent=err.message}}})();
