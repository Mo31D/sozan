{
  "name": "sozan-tutor-os",
  "version": "0.4.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "npm run check && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "check": "node --check src/worker.js && node --check src/wrapper.js && node --check public/app.js"
  },
  "devDependencies": { "wrangler": "^4.45.0" }
}
